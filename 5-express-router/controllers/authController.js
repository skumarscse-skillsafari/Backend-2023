export const loginUser = (req, res) => {
  res.status(200).send("<h1>LOGIN USER</h1>");
};

export const registerUser = (req, res) => {
  res.status(200).send("<h1>REGISTER USER</h1>");
};

export const userProfile = (req, res) => {
  res.status(200).send("<h1>USER PROFILE</h1>");
};
